<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppBaseTheme" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppLoadingTheme" parent="AppBaseTheme">
        <!--        <item name="android:background">@drawable/loading_launcher</item>-->
        <item name="android:windowFullscreen">true</item>
    </style>

    <!--values-v21。5.0 以上提供了 setStatusBarColor() 方法设置状态栏颜色。-->
    <style name="TranslucentTheme" parent="AppBaseTheme">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <!--Android 5.x开始需要把颜色设置透明，否则导航栏会呈现系统默认的浅灰色-->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="AppActivityTheme" parent="TranslucentTheme">

    </style>

    <style name="ControlActivityTheme" parent="TranslucentTheme">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="TabLayoutTextStyle" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">14sp</item>
        <item name="textAllCaps">false</item>
    </style>

</resources>
